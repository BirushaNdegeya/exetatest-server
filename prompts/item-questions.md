item-questions

id: (uuid) - autogenerated
question: string
item_course_id: string
options: ["bukavu", "goma"],
answer: "goma" -> 1

example sequelize

    id: {
      type: DataTypes.UUID,
      defaultValue: DataTypes.UUIDV4,
      primaryKey: true,
    },
    question: {
      type: DataTypes.STRING,
      allowNull: false,
    },
    item_course_id: {
      type: DataTypes.STRING,
      allowNull: false,
    },
    options: {
      type: DataTypes.ARRAY(DataTypes.STRING),
      allowNull: false,
    },
    answer: {
      type: DataTypes.INTEGER,  // Stores the index position (0, 1, 2, etc.)
      allowNull: false,
    },


-> Only the admin can create and edit and other people will read